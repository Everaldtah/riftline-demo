{
  "name": "riftline-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RIFTLINE — a browser-based first-person hero shooter demo built with Three.js. Original IP, procedural geometry only, single-player vs bots.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
